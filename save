#!/bin/bash
# Usage: ./save "what you did this session"
set -e
[ -z "$1" ] && { echo 'usage: ./save "what you did"'; exit 1; }
./log "$*" > /dev/null
git add -A
git commit -qm "$*"
git push -q
echo "saved and pushed · $*"
